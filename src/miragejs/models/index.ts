// import { Model } from 'miragejs'
// import { type ModelDefinition } from 'miragejs/-types'

// ??? => UserType or IUser
// const User: ModelDefinition<???> = Model.extend({})

export const models = {
  // user: User
}
